#include <iostream>
#include <clite.h>

#define MODE_CLITE

#ifdef  MODE_CLITE

structure test
begin
public:
	number pointer asshole = nullptr;
	string fuck = "ddsfdfas";
	float floaty = 0.3334444444444f;
	number fuckass = 3;
end;

class derived
{
public:

	number d;
};


class assclass publically_inherits_from derived
begin
public:

	assclass()
	{

	}
end;

main

		use std;
		test pointer tes = new test();
		test pointer test2 = construct(test);
		
	
	if 4 > 3 then

		cout << "test1" << endl;

		else

		cout << "how????????" << endl;
	failure
		end

	if 5 < 4 then failure end

	number shitterfucker = 4;
	number pointer fuckershitter = addressof shitterfucker;

	void* p = allocate(50);
	any d = allocate(34);

	dereferenced fuckershitter = 2;

	string wordswords = "assref";

	decimal castA = 0.003f;
	number castB = cast(number) castA;

	cout << dereferenced fuckershitter << endl;
	cout << "Hello World!" << endl;

	hold

	success
end

#endif //  MODE_CLITE
#ifdef MODE_NORMALCPP
	struct test
	{
	public:
		int* asshole;
		int fuckass;

	
	};
	int main()
	{
		using namespace std;
		test* tes = new test();

		if (4 > 3)
		{
			cout << "test1" << endl;
		}
		else
		{
			cout << "how????????" << endl;
			return -1;
		}

		int shitterfucker = 4;
		int* fuckershitter = &shitterfucker;
		*fuckershitter = 3;
		const char* wordswords = "assref";

		cout << *fuckershitter << endl;
		cout << "Hello World!" << endl;

		system("pause");

		return 0;
	}

#endif